- docs
- example scripts
- improve error messages on configuration errors
- let handlers decide dynamically how long to wait for a retry
- allow symbols for exchange, queue, and message names
- :attempts should automatically be at least :exceptions + 1
- review log levels in all logger statements
- figure out how to avoid busy loops for delayed messages
  (probably just store them in redis)
